@model Dozen2MVC.Models.CustomerIndexVM

@{
    ViewData["Title"] = "View Customer";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Add a Customer</a>
    
</p>
<div>
    <form asp-action="Search">
        <label asp-for="SearchName">
        </label>
        <input asp-for="SearchName" />
        <input type ="submit" class= "btn btn-primary" />
    </form>
</div>
<table class="table">
    <thead>
        <tr>
            <!--<th>-->
                @*@Html.DisplayNameFor(model => model.DrinkId)*@
            <!--</th>-->
            <th>
                Name
            </th>
            <th>
                Age
            </th>
            <th>
               PhoneNumber
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Customers) {  //this is razor syntax/ razor block
        <tr>
            <!--<td>-->
                @*@Html.DisplayFor(modelItem => item.CustomerId)*@
            <!--</td>-->
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CustomerId }) |
                @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
                @Html.ActionLink("Delete", "Delete", new {  id=item.CustomerId  })
            </td>
        </tr>
}
    </tbody>
</table>
